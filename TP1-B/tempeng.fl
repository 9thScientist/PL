%{
#include "./sds/sds.h"
%}
%option noyywrap
%x FUNC CODIGO
%%
<*>{{ { BEGIN FUNC; }
<*>}} { BEGIN CODIGO; }

<FUNC>\[% .* %\] {  }
<*>.|\n { ECHO; }

%%
int main (int argc, char* argv[]) {
    if(argc == 2)
        yyin = fopen(argv[1], "r");
    yylex();

    return 0;
}
